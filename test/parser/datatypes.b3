// RUN: %b3 parse --print "%s" > "%t"
// RUN: %diff "%s.expect" "%t"

// Zero-argument constructor
datatype Unit = Unit()

// Single-argument constructor
datatype Box = Box(value: int)

// Multiple-argument constructor
datatype Pair = Pair(fst: int, snd: bool)

// Multiple constructors
datatype Option = None() | Some(value: int)

// Recursive datatype
datatype List = Nil() | Cons(head: int, tail: List)

// Complex datatype with multiple constructors and fields
datatype Tree = Leaf(value: int) | Node(left: Tree, right: Tree, data: int)

// Nested datatype
datatype TreeList = TNil() | TCons(head: Tree, tail: TreeList)
datatype RoseTree = Leaf(value: int) | Node(children: TreeList)

// Add a type declaration to verify parsing continues correctly
type MyType

// Add a function to verify parsing continues correctly
function TestFunc(x: MyType): List
